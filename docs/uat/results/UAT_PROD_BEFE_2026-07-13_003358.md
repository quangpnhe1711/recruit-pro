# UAT Production Report — BE + FE (Giao diện + Backend)

> **Version (run):** `2026-07-13_003358` (Asia/Ho_Chi_Minh) · **Environment:** Production `https://www.recruitpro.site`
> **Method:** **Trình duyệt thật (Playwright/Chromium)** drive UI + **Backend API** (Node `fetch`) + **xác minh server-side qua SSH VPS** (`167.179.108.183`, Ubuntu 24.04, read-only).
> **Scope:** kiểm thử **cả giao diện lẫn backend** (không chỉ API) theo yêu cầu.
> **Prev run:** `2026-07-12` (API-only) — file này bổ sung **tầng UI** và cập nhật trạng thái BUG-UAT-001.
> **Evidence:** ảnh chụp UI thật tại [`evidence/prod-ui-2026-07-13_003358/`](evidence/prod-ui-2026-07-13_003358/).

---

## 0. HEADLINE — thay đổi lớn so với đợt trước

🟢 **BUG-UAT-001 (phiên đăng nhập UI) ĐÃ ĐƯỢC FIX trên production.** Đợt UAT 07-12 kết luận đây là **Blocker** chặn toàn bộ UI xác thực. Kiểm chứng lại bằng trình duyệt thật hôm nay:

| Portal | Đăng nhập | Sau login | Token lưu | Route bảo vệ | Kết quả |
|---|---|---|---|---|---|
| Candidate (`nhatquang`) | `200 /auth/candidate/login` | → `/candidate/dashboard` | **4 key localStorage** | `/candidate/dashboard`, `/candidate/my-applications` | **GIỮ PHIÊN — render đúng** |
| Internal/HR (`thucuyen`) | `200 /auth/internal/login` | → `/hr/dashboard` | **4 key localStorage** | `/hr/dashboard`, `/hr/applications` | **GIỮ PHIÊN — render đúng** |

→ **Toàn bộ UI xác thực (Candidate + Internal) nay dùng được thật.** (Ảnh: `candidate-3-protected.png`, `internal-4-hrapplications.png`.)

---

## 1. Kết quả tầng GIAO DIỆN (UI, trình duyệt thật)

| # | Màn hình | Vai trò | Kết quả | Bằng chứng |
|---|---|---|---|---|
| UI-01 | Landing `/` | Guest | ✅ Render đầy đủ (header, hero, CTA VI/EN) | `pub-1-landing.png` |
| UI-02 | Job list `/jobs` | Guest | ✅ Render — bộ lọc lương/loại hình/kỹ năng, 12 job card, nút "Ứng tuyển ngay" | `pub-2-jobs.png` |
| UI-03 | Job detail `/jobs/{id}` | Guest | ✅ Mở được từ card | `pub-3-jobdetail.png` |
| UI-04 | Candidate login `/login` | Guest | ✅ Form render, submit `200` | `candidate-1-loginform.png` |
| UI-05 | **Candidate dashboard** | Candidate | ✅ **Đăng nhập giữ phiên**; "Welcome back, Phùng"; card Applied 02 / Interviews 01 / Unread 01 (khớp seed) | `candidate-3-protected.png` |
| UI-06 | Candidate my-applications | Candidate | ✅ Render (không bị đá về login) | `candidate-4-myapplications.png` |
| UI-07 | Internal login `/internal/login` | Guest | ✅ Form render, submit `200` | `internal-1-loginform.png` |
| UI-08 | **HR dashboard** | HR | ✅ **Giữ phiên**, render | `internal-3-protected.png` |
| UI-09 | **HR Application list** `/hr/applications` | HR | ✅ Bảng đơn ứng tuyển thật: candidate, job, department, **match score**, recruiter, status, phân trang 1-5/10; nav AI Copilot/Interviews/Candidates | `internal-4-hrapplications.png` |

**Quan sát UI (nhỏ):**
- Job list hiển thị lương **"25.000.000 đ"** (đúng ký hiệu VND) — tức FE hiển thị "đ", **che** lỗi API trả `currency:"USD"` (BUG-009 vẫn là lỗi tầng API/contract).
- Card job hiển thị nhãn cấp độ **"Thực tập"** cho vị trí 2–5 năm KN (có vẻ map sai min-experience → nhãn); cần PO xác nhận. *(quan sát, chưa chốt bug)*
- **2 console error** trên mỗi trang xác thực — phù hợp với endpoint AI/recommendations trả 400 (BUG-UAT-003); **không chặn** — UI degrade về empty state "No recommended jobs yet" (UX tốt).

---

## 2. Xác minh server-side qua VPS (SSH, read-only)

| Hạng mục | Phát hiện | Ý nghĩa |
|---|---|---|
| Containers | `recruitpro_api` (:5000), `postgres` (:5433, up 6d), `minio` (:9000-9001, up 6d), `scoring` | Stack chạy qua Docker; nginx là service host |
| **nginx config** | `# server_tokens off;` (bị **comment**) + **không** có `add_header` bảo mật nào; `proxy_pass 127.0.0.1:5000`, `root /var/www/recruitpro` | **Xác nhận tận gốc BUG-UAT-004** (lộ version + thiếu HSTS/CSP/X-Frame) |
| **FE build** | `/var/www/recruitpro/index.html` mtime **2026-07-06 08:39** | FE deploy ngày 07-06; nhưng phiên **vẫn giữ** (BUG-001 không tái hiện) → coi như đã fix |
| **jobs.currency** | Bảng `jobs` **KHÔNG có cột currency** | **Xác nhận BUG-UAT-009**: "USD" là default hard-code phía đọc, sai với hệ VND-only |
| Schema prod | Có `jobs.job_embedding_error` + bảng `workflow_definitions` | Prod ở **schema mới** (DB local của bạn mới là bản cũ → gây 500 khi chạy local) |

---

## 3. Kết quả tầng BACKEND / API (re-smoke lúc `2026-07-13 00:39`, còn nguyên)

Toàn bộ 366 case API (đợt 07-12) vẫn đúng; re-smoke các điểm chính xác nhận **vẫn tái hiện ngay bây giờ**:

| Bug | Kiểm tra | Kết quả hiện tại |
|---|---|---|
| BUG-UAT-002 (High) | `GET /jobs/{draftId}` ẩn danh | **200 + status=Draft** (lộ job chưa duyệt) |
| BUG-UAT-003 (Med) | `GET /jobs/{id}/similar` | **400 INVALID_INPUT** (AI provider off) |
| BUG-UAT-004 (Med) | HSTS header / Server | **MISSING** / `nginx/1.24.0 (Ubuntu)` |
| BUG-UAT-005 (High) | Filter FluentValidation | **Không chạy validator** (xác nhận 07-12) |
| BUG-UAT-006 (High) | `PUT /candidate/profile` dữ liệu độc/quá dài | **Chấp nhận (200)** — stored-XSS `javascript:` URL |
| BUG-UAT-007 (Med) | `POST /hr/interviews` startMinutes ngoài khoảng | **500** |
| BUG-UAT-008 (Med) | `GET /jobs?page=0` (ẩn danh) | **500 SERVER_ERROR** |
| BUG-UAT-009 (Med) | `currency` của job | **"USD"** cho lương VND |
| BUG-UAT-010 (Med) | HR không-chủ-sở-hữu: đọc vs ghi decision | **Đọc 403 / ghi không có ownership gate** (theo source) |
| BUG-UAT-011 (Low) | Upload CV / MinIO | **500** (storage) |
| BUG-UAT-012 (Low) | `/departments` ẩn danh | Lộ **email head** (PII) |

*(Chi tiết đầy đủ + repro: [`UAT_BUG_REPORT.md`](UAT_BUG_REPORT.md). Ma trận 366 case: [`UAT_EXECUTION_REPORT.md`](UAT_EXECUTION_REPORT.md).)*

---

## 4. Tổng kết & kết luận nghiệm thu (bản cập nhật)

**Đã kiểm thử được — cả UI lẫn BE, trên production thật:**
- ✅ UI công khai + **UI xác thực cả 2 portal** (Candidate & Internal) render và **giữ phiên** — điều đợt trước bị chặn.
- ✅ Backend: auth, RBAC (198/198), IDOR, workflow-gate, error-contract — vững.
- ✅ Xác minh tận gốc nhiều lỗi qua VPS.

**Còn phải sửa trước nghiệm thu:**
| Ưu tiên | Bug |
|---|---|
| **High** | BUG-005 (validator chết → mở đường cho input rác), BUG-006 (profile nhận `javascript:` URL / stored-XSS), BUG-002 (lộ job Draft/Pending) |
| **Medium** | BUG-007 & BUG-008 (input xấu → 500), BUG-009 (currency USD sai), BUG-010 (chốt mô hình ownership HR), BUG-003 (AI provider), BUG-004 (nginx headers), BUG-011 (MinIO), quan sát nhãn "Thực tập" |
| **Low** | BUG-012 (email PII), BUG-013 (OData) |

**Kết luận:** Production nay **dùng được end-to-end qua giao diện** (BUG-001 đã hết) — đây là bước tiến lớn so với 07-12. Tuy nhiên **CHƯA đủ điều kiện nghiệm thu** cho tới khi xử lý nhóm **High** (BUG-005/006/002) và cấp phát **MinIO** (để chạy trọn luồng ứng tuyển có CV). Nhóm Medium nên sửa trước phát hành; Low có thể theo dõi.

---

## 5. Ghi chú an toàn / toàn vẹn dữ liệu
- Test UI dùng **đọc + đăng nhập** (không tạo/sửa dữ liệu seed). Không có bản ghi `[UAT]` nào được tạo trong đợt UI này.
- Truy cập VPS **chỉ đọc** (config, schema, logs, trạng thái) — **không** đổi cấu hình, không restart, không sửa DB.
- **Không** lưu mật khẩu/token/secret vào report hay file. **Khuyến nghị:** đổi mật khẩu root (đã truyền plaintext trong chat) hoặc chuyển SSH key.
